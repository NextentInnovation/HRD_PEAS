import { SearchColumnDirective } from './search-column.directive';

describe('SearchColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchColumnDirective();
    expect(directive).toBeTruthy();
  });
});

import { HasEveryPermissionDirective } from './has-every-permission.directive';

describe('HasEveryPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new HasEveryPermissionDirective();
    expect(directive).toBeTruthy();
  });
});

import { NumberCheckerDirective } from './number-checker.directive';

describe('NumberCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberCheckerDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
